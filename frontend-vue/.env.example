# Production / Amplify: set to CloudFront API URL from CDK output (must include /api/v1, no trailing slash)
VITE_API_BASE_URL=https://xxxxxxxxxxxxxx.cloudfront.net/api/v1

# Optional: subpath hosting on Amplify
# VITE_BASE_PATH=/
